# Práctica 1.2: Instalación básica de Apache

## 1. Instalación de Apache y verificación del servicio

Para realizar la instalación en el sistema operativo Ubuntu/Debian, actualizamos los índices de los paquetes e instalamos el paquete oficial `apache2`:

```bash
sudo apt update && sudo apt install apache2 -y
